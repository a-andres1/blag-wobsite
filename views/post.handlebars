<div class="container">
    <div class="column is-8 is-centered">
        <div class="content">
            <h1>{{post.title}}</h1>
            <p>{{post.body}}</p>
        </div>
        <textarea class="textarea is-warning" type="text" placeholder="Comment"></textarea>
        <div class="buttons">
            <button class="button is-warning mt-5">Submit</button>
        </div>
    </div>
</div>